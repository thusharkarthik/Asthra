[project]
name = "asthra-api-client"
version = "0.1.0"
description = "Lightweight Python API clients for Asthra services."
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]

[tool.setuptools.packages.find]
where = ["."]
