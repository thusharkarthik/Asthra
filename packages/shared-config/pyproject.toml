[project]
name = "asthra-shared-config"
version = "0.1.0"
description = "Shared configuration utilities for Asthra services."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
