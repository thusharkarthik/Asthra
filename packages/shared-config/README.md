# Asthra Shared Config

`packages/shared-config` contains lightweight environment and service configuration helpers for Asthra services.

It avoids heavy dependencies and does not contain secrets or business logic.

## Test

```bash
python -m pytest tests -q
```
