# Shared Packages

Shared reusable code for Asthra belongs in this directory.

Add a package here only when the code is genuinely reusable across app boundaries or the boundary is already clear from the current tier.
