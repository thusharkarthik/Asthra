[project]
name = "asthra-shared-auth"
version = "0.1.0"
description = "Lightweight shared auth utilities for Asthra downstream services."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
