[project]
name = "asthra-shared-observability"
version = "0.1.0"
description = "Lightweight observability helpers for Asthra services."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
