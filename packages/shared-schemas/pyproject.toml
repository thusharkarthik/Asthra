[project]
name = "asthra-shared-schemas"
version = "0.1.0"
description = "Cross-service generic schemas for Asthra."
requires-python = ">=3.11"
dependencies = ["pydantic>=2.0"]

[tool.setuptools.packages.find]
where = ["."]
