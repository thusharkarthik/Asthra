[project]
name = "asthra-shared-db"
version = "0.1.0"
description = "Shared database utilities for Asthra services."
requires-python = ">=3.11"
dependencies = ["sqlalchemy>=2.0"]

[tool.setuptools.packages.find]
where = ["."]
