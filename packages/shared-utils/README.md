# Asthra Shared Utils

`packages/shared-utils` contains lightweight, business-agnostic utility helpers for Asthra services.

It only uses the Python standard library and should remain small.

## Test

```bash
python -m pytest tests -q
```
