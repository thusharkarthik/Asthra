# Asthra Shared Test Utils

`packages/shared-test-utils` contains generic test helpers for Asthra services and packages.

It avoids business-specific factories and does not require live services.

## Test

```bash
python -m pytest tests -q
```
