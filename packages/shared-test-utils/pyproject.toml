[project]
name = "asthra-shared-test-utils"
version = "0.1.0"
description = "Shared test utilities for Asthra services and packages."
requires-python = ">=3.11"
dependencies = ["sqlalchemy>=2.0"]

[tool.setuptools.packages.find]
where = ["."]
