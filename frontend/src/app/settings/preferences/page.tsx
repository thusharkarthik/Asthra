"use client";

import { PlaceholderSettingsView } from "@/components/settings/settings-admin-views";

export default function PreferencesSettingsPage() {
  return <PlaceholderSettingsView title="Preferences" description="Theme, notification, shell, and workspace preference controls." />;
}
