# Session: 2026-06-28

## Fix 6 — Invite Modal Role Scoping
Added `groupedInviteModalRoles` IIFE to `settings-admin-views.tsx`. Global directory context shows only `scope === "platform"` roles; org/workspace context shows only `scope !== "platform"` roles. Prevents platform roles appearing in org-level invites and vice versa.

## Fix 7 — Onboarding Skip + Motivational Home
- Added `onSkip?: () => void` prop to `OnboardingGate`, shows "Skip for now" link.
- Extracted `OrgCreateForm` as a reusable private component; added `CreateOrgDialog` export.
- Shell (`asthra-shell.tsx`) tracks `skippedOnboarding` state. Redirect effect (before early returns) restricts skipped users to allowed paths (`/`, `/settings`, `/settings/*`). Sidebar receives `skippedUser` prop and filters nav items to only `/` and `/settings`.
- Home page (`app/page.tsx`) shows motivational layout (4 benefit cards + CTA + `CreateOrgDialog`) when `organizations.length === 0 && !is_superuser`.

## Fix 8 — Contextual Help Button (Stop-Motion Explainer)
- Created `frontend/src/lib/help-registry.ts` with 17 route entries, each with emoji icon steps.
- Created `frontend/src/components/platform/contextual-help.tsx` with stop-motion explainer: `AnimatedArrow` (CSS `width: 0→32px` transition), `StepCard` (translateX + scale + opacity), staggered timers (700ms initial + 850ms/step).
- Wired to `HelpCircle` button in shell; state managed via `helpOpen: useState<boolean>`.

## Task: Platform-Led Organization Onboarding
Platform Owner/Admin can now create an organization and assign an existing user as its owner directly, without any email flow.

**Backend (`services/core-service/`)**:
- `schemas/organization.py`: Added `PlatformOnboardCreate { name, description, owner_user_id }`. Added `owner_name: str | None = None` to `OrganizationRead`.
- `services/organization_service.py`: Added `platform_onboard()` — requires `settings.organization.create` at platform scope, validates owner user, creates org manually (not via `create_with_owner` so admin is `created_by_id` while owner is the designated user), adds `OrganizationMember(member_role="owner")`, `RoleAssignment(organization_owner)`, in-app `Notification` to owner, `ActivityLog`. Also added `_attach_owner_names()` batch method called from `list()` — single JOIN query populates `org.owner_name` without N+1.
- `api/v1/organizations.py`: Added `POST /organizations/platform-onboard` (registered before `/{organization_id}` to avoid path conflict).

**Frontend (`frontend/src/`)**:
- `types/core.ts`: Added `owner_name?: string | null` to `Organization`.
- `services/api/settings-api.ts`: Added `platformOnboardOrganization()`.
- `components/settings/settings-admin-views.tsx` (`OrganizationsView`):
  - `canPlatformOnboard` detection via `is_superuser || roles.some(platform_owner/platform_admin)`.
  - "Onboard Organization" `<Button variant="outline">` visible to platform admins.
  - `usersQuery` lazy-loaded (enabled only when modal open + canPlatformOnboard).
  - Onboard dialog with name, description, `SettingsMemberSelect` for owner (required).
  - Org table gains "Owner" column — shows `owner_name` or muted "No owner assigned".
  - `platformOnboardMutation` success toast: `"${org.name} created. ${ownerName} has been assigned as Owner."`.

**Build**: `npm run build` EXIT:0, no TypeScript errors.
