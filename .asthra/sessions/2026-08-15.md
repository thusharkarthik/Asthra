# 2026-08-15

## Core Personal Settings Certification

- Audited Core personal settings backend routes, schemas, repository/service behavior, Settings Profile page, Settings Account page, and current Preferences/Notifications/Security pages.
- Confirmed personal profile/account endpoints are authenticated, self-owned, and independent of org/workspace/project bottom-bar context.
- Added `services/core-service/tests/test_personal_settings_certification.py` covering auth required, self profile update, sensitive/privilege field rejection, password rules, login after password change, self-deactivation, only-superuser deactivation protection, unrelated user profile denial, and RBAC mutation safety.
- Created `.asthra/reports/CORE_PERSONAL_SETTINGS_CERTIFICATION.md` with current flow, frontend state, known gaps, manual QA checklist, and certification result.
- Documented that Preferences are currently local/browser-only and Notifications/Security are placeholders pending future server-backed capabilities.

Validation:

- Targeted pytest will be attempted next; if local pytest is missing, syntax compile will be used as the fallback.
- `git diff --check`, `git status`, and `git diff --stat` will be run before final response.

## Core Scope Lifecycle Certification

- Audited Organization, Workspace, and Project backend models, schemas, repositories, services, API routes, permission checks, context version behavior, and activity producers.
- Audited Settings hierarchy frontend pages, create/edit/archive/restore affordances, route-scoped detail behavior, permission gates, and invalidation paths.
- Added `services/core-service/tests/test_scope_lifecycle_certification.py` covering auth required, organization onboarding/lifecycle, workspace lifecycle, project lifecycle, scope isolation, view-only denial, context version/platform context behavior, RBAC mutation safety, and audit action coverage.
- Created `.asthra/reports/CORE_SCOPE_LIFECYCLE_CERTIFICATION.md` with lifecycle architecture, permission matrix, frontend behavior, test matrix, known gaps, manual QA checklist, and go/no-go criteria.
- No production backend/frontend behavior, RBAC codes, navigation/sidebar, God Mode, bottom bar, or feature flag defaults were changed.

Validation:

- Targeted pytest was attempted but blocked because local `pytest` is not installed.
- In-memory Python syntax compile for the new scope lifecycle test passed.
- `git diff --check`, `git status`, and `git diff --stat` will be run before final response.
