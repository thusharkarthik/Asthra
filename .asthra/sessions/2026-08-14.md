# 2026-08-14

## Step 8 Navigation Locked Access UX

- Added locked sidebar item UX for the feature-flagged Role Navigation Config path. Locked items now show a lock affordance and open an `Access restricted` modal instead of only exposing a browser title tooltip.
- The modal shows item label, route, mode, role, nav key, and missing permission codes when available.
- Reused the existing Core `POST /notifications/access-request` handoff with a prefilled message containing navigation context. No new backend access-request system was added.
- Resolver metadata now includes `navigationConfigMissingPermissions` for locked items, and the targeted resolver test was updated to assert that metadata.
- `core.navigation_config.enabled=false` remains baseline; no `AsthraShell`, God Mode, bottom bar, backend RBAC, or feature-flag default changes were made.

Validation:

- `git diff --check` passed.
- Targeted frontend `vitest` was blocked because `frontend/node_modules/.bin/vitest` is missing.
- Frontend `tsc --noEmit` was blocked because `frontend/node_modules/.bin/tsc` is missing.

Manual QA required:

- Enable `core.navigation_config.enabled`, configure Organization Member / Organization mode / Members = `show_locked_if_denied`, login as a member missing Members permission, confirm the locked item opens the explanation modal, request access sends, and no navigation occurs.
- Disable the flag and confirm the baseline sidebar returns unchanged.

## Core Access Request Certification

- Audited the current Request Access flow used by restricted Settings pages and locked navigation items.
- Confirmed the backend API is `POST /api/v1/notifications/access-request` and currently accepts `page` plus optional `message`.
- Confirmed the service creates an `access_request` notification for an org admin/owner, platform admin/owner, or superuser fallback, and does not mutate permissions or role assignments.
- Added `services/core-service/tests/test_access_request_certification.py` covering auth required, org admin notification, platform fallback, no-recipient safe failure, no access grant, context preservation in notification message, and duplicate-request behavior.
- Created `.asthra/reports/CORE_ACCESS_REQUEST_CERTIFICATION.md` with flow audit, test matrix, safety statement, manual QA checklist, and known gaps.

Validation:

- Targeted pytest commands were blocked because local Python has no `pytest` installed.
- Python syntax compile for the new test file passed using `PYTHONDONTWRITEBYTECODE=1 compile(...)`.
- Frontend typecheck was not run because no frontend files changed in this certification pass beyond the prior Step 8 changes already documented.

## Core Feature Flags Certification

- Audited the Core Feature Flag Engine, API routes, platform context integration, and Settings Feature Flags frontend page.
- Confirmed `core.navigation_config.enabled` is a system Core flag, defaults to false, and is only changed through explicit scoped overrides.
- Confirmed feature flag management is permission-protected with `settings.feature_flags.view` / `settings.feature_flags.manage`; effective flag reads are available to authenticated users for context consumption.
- Added `services/core-service/tests/test_feature_flags_certification.py` covering default seeding, fail-closed behavior, platform/org override precedence, API authz, platform context exposure, no RBAC mutation, and validation errors.
- Created `.asthra/reports/CORE_FEATURE_FLAGS_CERTIFICATION.md` with the certified flow, defaults, permissions behavior, platform context behavior, tests, known gaps, and manual QA checklist.

Validation:

- Targeted pytest was attempted but blocked because local Python has no `pytest` installed.
- Python syntax compile for the new certification test file passed using `PYTHONDONTWRITEBYTECODE=1 compile(...)`.
- `git diff --check` passed.

## Core Notifications Certification

- Audited Core notification model, schemas, repository, service, API routes, frontend notification bell/popover, local notification store, and Access Request notification integration.
- Confirmed notification ownership is enforced by `current_user.id` for list/get/mark/delete and that mark-all updates only the current user's unread notifications.
- Confirmed Access Request creates `access_request` notifications with requester/page/context text and `entity_type=user_profile`, without mutating permissions, role assignments, feature flags, or navigation config.
- Added `services/core-service/tests/test_notifications_certification.py` covering auth required, empty state, recipient isolation, newest-first listing/fields, mark-read idempotency, unread count behavior, mark-all isolation, and access-request safety.
- Created `.asthra/reports/CORE_NOTIFICATIONS_CERTIFICATION.md` with architecture audit, frontend behavior, access-request integration, target/link behavior, test matrix, manual QA checklist, known gaps, and go/no-go criteria.

Validation:

- Targeted pytest commands were attempted but blocked because local Python has no `pytest` installed.
- Python syntax compile for the new notification certification test passed using `PYTHONDONTWRITEBYTECODE=1 compile(...)`.
- Frontend type validation was not run because no frontend files changed.
- `git diff --check` passed.
