# Session: 2026-06-29

## Task: Profile Page, Account Page, Preferences Page

Built fully functional Profile, Account, and Preferences settings pages.

### Backend (`services/core-service/`)

**`schemas/user.py`**:
- Added `ChangePasswordPayload { current_password: str, new_password: str = Field(min_length=8) }`

**`services/user_service.py`**:
- Added `change_password()`: verifies current password, rejects if new == current, hashes and saves new password, logs activity
- Added `deactivate_me()`: blocks last active superuser, sets `is_active=False`, logs activity
- Imported `hash_password, verify_password` from `app.core.security`
- Imported `ChangePasswordPayload` from schemas

**`api/v1/me.py`** (full rewrite):
- `GET /me` → `UserProfileRead` (fresh profile fetch)
- `PATCH /me` → `UserProfileRead` (update full_name, job_title, timezone, locale, avatar_url)
- `POST /me/change-password` → 204 (change password)
- `POST /me/deactivate` → 204 (deactivate own account)
- `GET /me/permissions` → kept, registered last to avoid path conflict

### Frontend (`frontend/src/`)

**`services/api/settings-api.ts`**:
- Added `getMyProfile()` → `GET /me`
- Added `updateMyProfile()` → `PATCH /me`
- Added `changePassword()` → `POST /me/change-password`
- Added `deactivateMyAccount()` → `POST /me/deactivate`

**`app/settings/profile/page.tsx`** (full implementation):
- Avatar: initials in deterministic color circle (8 colors, seeded from name/email charCode)
- Personal Info form: full_name (editable), email (read-only), job_title, timezone select (20 zones), locale select (9 languages)
- Form initialized from `profileQuery.data ?? currentUser` via `useEffect`
- Save: `PATCH /me` → `useAuthStore.setState` to update store in-place → success toast
- Role Assignments section: `listRoleAssignments({ user_id, status: "active" })` + `listRoles()` cross-referenced for role names; `usePlatformContext()` for org/workspace name lookups; shows scope badge + role name + scope label

**`app/settings/account/page.tsx`** (full implementation):
- Account Info card: email, full_name, status badge (emerald=active), member since (formatted date), superuser badge
- Change Password form: current + new + confirm fields, strength meter (score 0-5 → Weak/Fair/Good/Strong with color bar), client-side validation before API call, shows "passwords don't match" inline
- Danger Zone: "Deactivate Account" button → inline overlay modal with "DEACTIVATE" confirmation text input; submit button disabled until text matches; on success → logout()

**`app/settings/preferences/page.tsx`** (replaced placeholder):
- Appearance: Light/Dark/System theme toggle buttons (via `next-themes` `useTheme`)
- Notifications: in-app toggle (functional), email + digest (disabled, coming soon)
- Preferences saved to localStorage under `asthra-notification-prefs`
- Language: card linking to Profile page

### Build
`npm run build` → EXIT:0, 137 static pages generated, no TypeScript errors.

## Task: Org Setup Checklist (Home Page)

Created `frontend/src/components/platform/org-setup-checklist.tsx` — a setup progress checklist shown to org owners/admins who haven't completed basic org setup.

- 5 checklist items: workspace, project, invite member, assign roles, work item in Flow (coming soon)
- Progress bar with percentage
- Dismiss (permanent per org via localStorage: `asthra_setup_checklist_dismissed_${orgId}`)
- Minimize (per org via localStorage: `asthra_setup_checklist_minimized_${orgId}`)
- Hydration guard (`useState(false)` + `useEffect`) to prevent SSR/localStorage mismatch
- Collapses to emerald "You're all set!" banner when all complete
- Authority check: `permissions?.roles?.some(r => r.key in [organization_owner, organization_admin] && r.scope === "organization")`
- Mounted in `frontend/src/app/page.tsx` after error state, before `PlatformSetupGuide`

## Task: Profile Page Bug Fix (stale form after save)

Root cause: `useEffect` deps `[profileQuery.data, currentUser]` — stale `profileQuery.data` (cached) took precedence over updated auth store after PATCH /me succeeded.

Fix: In `onSuccess`, call `queryClient.setQueryData(["me", "profile"], updatedUser)` to synchronously update cache before useEffect re-runs. Also update form state directly + update auth store.

## Task: API Key Management UI

Built full API key management page at `/settings/api-keys`.

**Backend**: All endpoints already existed and were complete. No backend changes needed.
- `POST /api-keys` (create + return raw key once), `GET /api-keys` (list), `PATCH /api-keys/{id}` (update name), `DELETE /api-keys/{id}` (hard delete), `POST /api-keys/{id}/revoke` (deactivate)
- Key format: `ak_{token_urlsafe(32)}`, prefix = first 12 chars, SHA256 hash stored

**`settings-api.ts`**:
- Added `expires_at?: string | null` to `createApiKey` payload type

**`app/settings/api-keys/page.tsx`** (replaced placeholder `ApiKeysView` re-export):
- Self-contained implementation with full UI
- **Create modal**: Name (required), Scope (read/write/admin select), Expiry (30d/90d/1yr/Never select). Expiry computed as ISO datetime on frontend.
- **Key reveal modal**: Raw key displayed with monospace code block. Copy-to-clipboard button (updates to green checkmark on copy). "I have saved this key" checkbox. Close button disabled until key is copied or checkbox checked. Key cleared from state on close — never visible again.
- **Table**: Name, Prefix (`ak_XXXXX…`), Scope, Created (date), Expires (relative: "in Xd", "today", "Never", "Expired Xd ago"), Last Used (date or "Never"), Status badge, Revoke action.
- **Status badges**: Active (emerald), Expired (amber), Revoked (rose) — computed from `is_active` + `expires_at` comparison.
- **Revoke flow**: Click Revoke → confirmation dialog with key name, warning message, Cancel + Revoke (destructive) buttons.
- Empty/loading/error states using `EmptyModuleState`, `ErrorState`.

## Task: Organization & Workspace Settings Pages

Extended org and workspace settings to support full configuration via UI.

### Backend

**`services/core-service/app/schemas/settings.py`**:
- Added to `OrganizationSettingsRead/Update`: `domain`, `website_url`, `industry`, `logo_url`, `primary_color`, `locale`, `date_format`
- Added to `WorkspaceSettingsRead/Update`: `visibility`, `locale`, `enabled_modules: list[str]`
- Added `ALL_MODULES` constant: `["flow", "docs", "discover", "desk", "pulse", "collab", "automation"]`
- `WorkspaceSettingsRead.enabled_modules` defaults to all modules via `Field(default_factory=...)`

**`services/core-service/app/services/settings_service.py`**:
- `DEFAULT_ORGANIZATION_SETTINGS`: added `domain`, `website_url`, `industry`, `logo_url`, `primary_color`, `locale`, `date_format` (all default `None`)
- `DEFAULT_WORKSPACE_SETTINGS`: added `visibility` (`"private"`), `locale` (`None`), `enabled_modules` (all 7 modules)
- No model column changes, no migrations — all stored in existing `settings: Mapped[dict | None] = mapped_column(JSON)` column

### Frontend

**`services/api/settings-api.ts`**:
- Added `OrgSettingsRecord` type (domain, website_url, industry, logo_url, primary_color, locale, date_format + existing operational fields)
- Added `WorkspaceSettingsRecord` type (visibility, locale, enabled_modules + existing fields)
- Added `getOrganizationSettings(token, orgId)` → `GET /organizations/{id}/settings`
- Added `updateOrganizationSettings(token, orgId, payload)` → `PATCH /organizations/{id}/settings`
- Added `getWorkspaceSettings(token, wsId)` → `GET /workspaces/{id}/settings`
- Added `updateWorkspaceSettings(token, wsId, payload)` → `PATCH /workspaces/{id}/settings`

**`app/settings/organizations/[id]/page.tsx`** (replaced `OrganizationDetailView` re-export):
- Full `"use client"` page using `useParams` for orgId
- `OrgTabs` component: inline tab nav using `Link` (Overview/Members/Workspaces/Roles/Permissions) since `AdminTabs` in settings-admin-views is not exported
- Sections: Overview card, General form (name/desc → PATCH /organizations/{id}), Identity & Contact (domain/website/industry), Branding (logo URL with live preview img + color picker), Localization (timezone/locale/date format) — last 3 sections combined in one Settings form → PATCH /organizations/{id}/settings
- Danger Zone: deactivate with inline confirm (2-step)
- Authorization: `isAuthorized` from `useSettingsAuthority()` — superuser/platform/org authority grants edit; fields disabled when not authorized

**`app/settings/workspace/page.tsx`** (replaced `WorkspaceContextSettingsView` re-export):
- Full `"use client"` page using `useWorkspaceStore.selectedWorkspaceId` + `usePlatformContext().workspaces`
- Empty state when no workspace selected
- Sections: General (name/desc → PATCH /workspaces/{id}), Access & Localization (visibility/timezone/locale), Module Visibility (7 checkboxes) — last 2 in single Settings form → PATCH /workspaces/{id}/settings
- Danger Zone: archive workspace (2-step confirm)
- `isAuthorized`: superuser/platform/org levels always authorized; workspace level authorized only if `authorityWsId === wsId`

### Build
`npm run build` → ✓ Compiled successfully, EXIT:0. `/settings/organizations/[id]` (10 kB), `/settings/workspace` (9.34 kB). No TypeScript errors.

## Task: Permission Transparency + Notification Accept/Decline + Checklist Invite Fix

Three bundled features. Build: ✓ Compiled, 137 pages, no TypeScript errors.

### Backend

**`services/invitation_service.py`**:
- Changed `type="invitation.created"` → `type="invitation.pending"` for invitee notifications (matches frontend type check)
- Added `accept_in_app(invitation_id, current_user)`: accepts pending invitation without email token (user already authenticated); verifies email match, same membership/role logic as `accept()`, fires `invitation.accepted` notification to inviter

**`api/v1/invitations.py`**:
- Added `POST /{invitation_id}/accept-in-app` → calls `InvitationService.accept_in_app()`

**`services/notification_service.py`**:
- Added imports: `Role`, `RoleAssignment` (joined query), `UserRepository`
- Added `send_access_request(page, message, current_user)`: finds admin via `_find_admin_for_user()`, creates `type="access_request"` notification with `entity_type="user_profile"`, `entity_id=str(current_user.id)`
- Added `_find_admin_for_user(current_user)`: 3-priority admin resolution — (1) org admin/owner in user's orgs, (2) platform admin/owner, (3) any active superuser

**`schemas/notification.py`**:
- Added `AccessRequestCreate { page: str, message: str | None = None }`

**`api/v1/notifications.py`**:
- Added `POST /notifications/access-request` endpoint (registered BEFORE `GET ""` to avoid FastAPI routing collision)

### Frontend

**`services/api/settings-api.ts`**:
- Added `acceptInvitationInApp(token, invitationId)` → `POST /invitations/{id}/accept-in-app`
- Added `sendAccessRequest(token, payload)` → `POST /notifications/access-request`

**`components/platform/notification-center.tsx`** (rewritten):
- Added `entity_id: item.entity_id` to coreNotification map
- `"entity_id" in item` guard for TypeScript compat (in-store `NotificationItem` type doesn't have `entity_id`)
- `acceptInvitationMutation`: on success invalidates `["core", "notifications"]`, `["members-page"]`, `["organizations"]`, `["permissions.all"]`; toast + close
- `declineInvitationMutation`: uses `revokeInvitation`; on success invalidates notifications + toast
- Type check: `"invitation.pending" || "invitation.created"` for backward compat with existing DB rows
- `notificationHref`: added `entity_type === "user_profile"` → `/settings/members/${id}`

**`app/settings/layout.tsx`**:
- Exported `RequestAccessButton` component — modal with optional message textarea, calls `sendAccessRequest`, shows confirmation on success
- Restricted state now includes `action={<RequestAccessButton page={pathname} />}`

**`app/settings/members/page.tsx`** and **`app/settings/members/[id]/page.tsx`**:
- Imported `RequestAccessButton` from layout
- Restricted state `action` prop updated

**`components/platform/org-setup-checklist.tsx`**:
- Invite href: `/settings/members` → `/settings/members?action=invite&orgId=${orgId}`

**`components/settings/settings-admin-views.tsx` (MembersView)**:
- Added `useSearchParams` import from `next/navigation`
- Added `const searchParams = useSearchParams()` + `useEffect` on mount: reads `?action=invite&orgId=N`, calls `setInviteOpen(true)` and `setInviteOrgId(N)`

## Task: Audit Log Consistency (Core Service)

Made audit logging consistent across all Core service actions. Every action now logs: WHO (actor_user_id + name in description), WHAT (action string), WHAT TO (entity_type + entity_id), WHEN (timestamp via TimestampMixin), CONTEXT (organization_id, workspace_id where applicable), DESCRIPTION (human-readable sentence).

### Backend changes

**`organization_service.py`**:
- Replaced direct `ActivityLog` model usage with `ActivityService.log_activity()`
- `create()`: added log + fixed missing `self.db.commit()` + `self.db.refresh(organization)` (was relying on end-of-request session commit, unlike all other services)
- `onboard()`: log with action `organization.created` (via self-serve onboarding)
- `platform_onboard()`: renamed action from old direct model to `platform.org_onboarded`
- `update()`: captures `permission_code` before update to derive correct action (deactivated/reactivated/updated)
- `delete()`: log with `organization.deactivated`

**`workspace_service.py`**: `create` (`workspace.created`), `update` (archived/restored/updated), `delete` (`workspace.archived`)

**`project_service.py`**: `create` (`project.created`), `update` (archived/restored/updated), `delete` (`project.archived`). Org scope via `project.workspace.organization_id`.

**`team_service.py`**: Added ActivityService import + logs for `create` (`team.created`), `update` (`team.updated`), `delete` (`team.deleted`), `add_member` (`member.added`), `remove_member` (`member.removed`). Org scope via `team.workspace.organization_id`.

**`invitation_service.py`**: Renamed actions `invitation.*` → `member.*` (member.invited, member.invitation_accepted, member.invitation_cancelled, member.invitation_resent). Descriptions updated to include actor name.

**`role_service.py`**: Added logs for `assign_user_role` (`role.assigned`, with `self.db.flush()` before log to ensure assignment.id is populated), `update` (`role.updated`), `delete` (`role.deleted`), `link_permission` (`permission.assigned`), `unlink_permission` (`permission.removed`, looks up permission before unlinking for label).

**`user_service.py`**: Improved `update_me()` description from `"User {id} profile was updated"` to `"{actor_name} updated their profile ({field_names})"`.

### Frontend changes

**`services/api/settings-api.ts`**:
- Added `ActivityLogRecord` type export
- Added `listActivityLogs(token, params)` → `GET /api/core/api/v1/activity` with query param serialization

**`app/settings/audit-logs/page.tsx`** (full implementation replacing placeholder):
- Access control: `can("guard.audit.view")` OR `authorityLevel` is org/superuser/platform
- Scope: global (no org filter) for superuser/platform; org-scoped for org authority or auditor role
- Filter: predefined `AUDIT_ACTIONS` dropdown (all known action strings)
- Table: Timestamp (relative, absolute on hover), Actor (resolved from users list via userMap), Action (color badge by entity prefix — org=blue, workspace=purple, project=green, team=yellow, member=orange, role=rose, permission=pink, user=gray, platform=indigo), Description, Scope
- Pagination: 25 per page, prev/next buttons, `hasMore = logs.length === PAGE_SIZE`
- Empty/loading/error states with appropriate UI components

## Task: Three-Mode Navigation

Built automatic navigation mode detection — sidebar and bottom bar now adapt to the user's authority level.

### New file: `frontend/src/lib/navigation-mode.ts`

- `NavigationMode = "platform" | "org" | "work"` type
- `detectNavigationMode(isSuperuser, roles)`: superuser or platform_owner/admin/support → platform; org roles → org; else → work
- `autoDetectModeFromPath(pathname)`: for superuser route-based auto-detection (work module paths → work, `/settings/organizations/{N}` → org, else → platform)
- `PLATFORM_NAV`, `ORG_NAV`, `WORK_NAV` — three `ModeNavSection[]` configs
- `navSectionsForMode(mode)` — selector function

### Updated: `frontend/src/components/navigation/sidebar-nav.tsx` (rewritten)

- Removed `navSections` from `nav-items.ts` import (replaced by mode-based sections)
- Calls `usePlatformContext()` directly for `can()` + org/workspace names
- Props: added `navigationMode`, `isSuperuser`, `modeOverride`, `onModeOverride`
- Mode indicator at top: icon + label (Platform Mode / org name / workspace name)
- Superuser mode switcher: `<select>` with Auto/Platform/Organization/Work options (hidden when collapsed)
- Renders `navSectionsForMode(effectiveMode)` with `shouldShowItem()` filtering (permission + skippedUser)
- Work mode permission items: `can(item.permission)` hides items when user lacks permission

### Updated: `frontend/src/layouts/asthra-shell.tsx`

- Imports `detectNavigationMode`, `autoDetectModeFromPath`, `NavigationMode`
- Added `superuserModeOverride: useState<NavigationMode | null>(null)` — manual override for superusers
- `baseNavigationMode`: computed from `detectNavigationMode(isSuperuser, permissions?.roles ?? [])`
- `autoDetectedMode`: for superuser, uses `autoDetectModeFromPath(pathname)`; for others, equals `baseNavigationMode`
- `navigationMode`: `superuserModeOverride ?? autoDetectedMode`
- `PermissionAwareSidebar` updated to pass new mode props
- Bottom bar: `showOrgSwitcher = mode !== "platform"`, `showWorkspaceSwitcher = showProjectSwitcher = mode === "work"`
- Platform Mode shows "Platform Mode" text label in bottom bar instead of selectors

### Build
`npm run build` → ✓ Compiled, 137 pages, EXIT:0. No TypeScript errors.

## Session: Permission Simulator (2026-06-29)

### Task
Build Permission Simulator — Superuser and Platform Owner can click "View As" and see the UI exactly as any role would see it.

### Files Changed
- `services/core-service/app/api/v1/access_control.py` — Added `GET /access-control/simulate?role_key=X` endpoint (read-only, superuser/platform_owner/platform_admin only)
- `frontend/src/lib/permission-simulator.ts` (new) — Zustand store + `roleKeyToNavigationMode()` helper
- `frontend/src/context/platformContext.tsx` — `can()` overrides to use simulated permissions; subscribes to `useSimulationStore`
- `frontend/src/layouts/asthra-shell.tsx` — Simulation banner above content area; `effectiveNavigationMode` drives sidebar and bottom bar
- `frontend/src/components/navigation/sidebar-nav.tsx` — "View As" button + inline panel (role selector + Start/Exit), active simulation indicator
- `frontend/src/services/api/settings-api.ts` — `simulatePermissions()` API method

### Build Result
`npm run build`: exit 0. `tsc --noEmit`: exit 0. No TypeScript errors.

### Not Implemented
"View As This User" in `/settings/members/[id]` — excluded per task spec (settings pages off limits). Backend already supports `?user_id=N`; frontend wiring deferred.
