# 2026-07-12

## Org-Scoped Member Invite / Detail Stabilization

Fixed Settings organization member flows so explicit route scope is preserved:

- `/settings/organizations/:orgId/members` invite now defaults and locks `organization_id` to the route org.
- Organization-scoped invite hides platform roles and filters workspace choices to the route organization.
- Member row detail links from org members now route to `/settings/organizations/:orgId/members/:memberId`.
- Added a thin org-scoped member detail route that reuses `MemberDetailView` with explicit organization scope.
- Add Role from org-scoped member detail hides platform roles, defaults organization scope to the route org, and filters workspace choices to descendants of that org.
- Global `/settings/members/:memberId` remains unchanged for cross-scope administration.
- Active organization settings page now exposes Restore for inactive organizations and gates lifecycle actions with `settings.organization.archive` / `settings.organization.restore` instead of role-name checks.

Validation:

- `./node_modules/.bin/tsc --noEmit` passed.
- `npm run build` in the repository failed because `frontend/.next` contains pre-existing `nobody:nogroup` artifacts that the current user cannot unlink.
- `npm run build` passed from a clean `/tmp/asthra-frontend-verify` frontend copy with repo `node_modules` symlinked.

Manual QA still needed:

- Invite from org members confirms org is preselected/locked and payload uses route org.
- Member detail from org members stays under the org route.
- Add Role in org-scoped member detail succeeds for org roles without org selection.
- Archive/restore org actions appear only for users with explicit lifecycle permissions.
