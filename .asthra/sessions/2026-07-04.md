
## Task 7 — OpenAPI TypeScript Client Generation [COMPLETED]

**Goal**: Add `@hey-api/openapi-ts` code generation step that produces a typed TypeScript client from the core service OpenAPI spec.

**Blockers resolved**:
- `@hey-api/openapi-ts@0.99.0` hard-enforces Node >=22; environment is Node 18.19.1 → downgraded to `@hey-api/openapi-ts@0.49.0`
- Corrupt node_modules (0-byte files) from failed install → `npm cache clean --force` + reinstall
- `@hey-api/client-fetch@0.13.1` incompatible with v0.49-generated code (`client` vs `createClient` API) → switched to built-in `client: 'fetch'`; removed `@hey-api/client-fetch` from dependencies

**Files changed**:
- `frontend/openapi-ts.config.ts` (new) — config: built-in fetch, localhost:8000, output to src/services/api/generated
- `frontend/package.json` — added `generate:api`, `prebuild`, `predev` scripts; removed `@hey-api/client-fetch`; `@hey-api/openapi-ts@0.49.0` confirmed in devDeps
- `frontend/.gitignore` — added `src/services/api/generated/`
- `frontend/src/services/api/index.ts` (new) — barrel: `export * from './generated'`
- `.asthra/platform.json` — added `generate:api` note to `beforeFinishing`
- `.asthra/memory/state.md` — added OpenAPI generation section
- `.asthra/memory/decisions.md` — added v0.49/built-in-fetch decision
- Generated (gitignored): `src/services/api/generated/{index.ts,types.gen.ts,services.gen.ts,schemas.gen.ts,core/}`

**Build**: Passed. 98 endpoints, all TypeScript clean.

## Task 8 — Convention-Based Help Registry [COMPLETED]

**Goal**: Make the contextual help (Info button) registry convention-based. Auto-register routes from the module registry; keep human-written content in a separate map; new modules appear as placeholders instead of silently missing.

**Files changed**:
- `frontend/src/lib/convention-help-registry.ts` (new) — `HelpContent`/`HelpStep`/`HelpCTA` interfaces; `HELP_CONTENT` (24 routes, all existing + new: assistant, memory, organizations, workspaces, access-control, audit-logs, api-keys); `buildHelpRegistry(availableModules)` merges HELP_CONTENT with auto-placeholders; `findHelpContent(registry, pathname)` exact/parent-chain lookup
- `frontend/src/lib/help-registry.ts` (deprecated) — replaced with re-exports from convention-help-registry; backward compat preserved
- `frontend/src/components/platform/contextual-help.tsx` (updated) — switched to `buildHelpRegistry`/`findHelpContent`; uses `usePlatformContext().availableModules`; `useMemo` for registry and content

**Build**: Passed. Stop-motion animation unchanged. All 24 routes have human content; new modules auto-placeholder.
