
## Task 15 — Permission Schema System [COMPLETED]

**Goal**: Declarative permission schema for all settings pages. A `GodModeSchemaPanel` that shows ALL schema-defined elements (including hidden ones never rendered), complementing the auto-overlay which only shows elements actually can()-checked.

**Files created**:
- `frontend/src/lib/permission-schema.ts` — `PageSchema`, `PageSchemaElement` types + `PAGE_SCHEMAS` (9 pages) + `findSchemaForRoute(pathname)`
- `frontend/src/hooks/use-page-permissions.ts` — `usePagePermissions()` hook. Returns `{ schema, visible }`. In God Mode edit mode: proactively registers ALL schema permissions via `useGodModeTracker.getState().registerPermissionCheck`.
- `frontend/src/components/platform/schema-gate.tsx` — `SchemaGate({ elementKey })` — looks up permission from schema, behaves like `PermissionGate` in edit mode (+/- overlay).
- `frontend/src/components/platform/god-mode-schema-panel.tsx` — `GodModeSchemaPanel`. Fixed `right-4 top-16` panel. Shows all schema elements with type icons, Eye/EyeOff visibility indicators, +/- buttons for permissioned elements. Calls `usePagePermissions()` internally.

**Files modified**:
- `frontend/src/layouts/asthra-shell.tsx` — imports and mounts `<GodModeSchemaPanel />` alongside `<GodModeAutoOverlay />`
- `frontend/src/app/settings/organizations/[id]/page.tsx` — replaced `PermissionGate` with `SchemaGate` for all 5 OrgTabs + 2 save buttons; added `usePagePermissions()` call; removed `PermissionGate` import
- `frontend/src/app/settings/members/page.tsx` — added `usePagePermissions()` call
- `frontend/src/app/settings/organizations/page.tsx` — added `usePagePermissions()` call

**TypeScript**: `npx tsc --noEmit` — passed (exit code 0)

**Build**: `npm run build` — passed (exit code 0)

**Key design decisions**:
1. `SchemaGate` replaces `PermissionGate` in migrated pages — permission code lives in schema, not in page JSX
2. Schema panel is globally mounted → works for all 9 schema pages even unmigrated ones
3. Per-page `usePagePermissions()` calls are additive (idempotent) — self-documentation only
4. `PAGE_SCHEMAS` covers all 9 settings pages ahead of migration to give full panel coverage immediately

---

## Task 16 — God Mode Schema Panel Fixes + Page Migrations [COMPLETED]

**Goal**: Fix panel overlap, button visibility, SchemaGate border highlight, collapsible panel, migrate remaining settings pages.

**Fix 1 — Panel XOR**: `asthra-shell.tsx` — `hasSchema = findSchemaForRoute(pathname)`; show `<GodModeSchemaPanel />` when schema exists, `<GodModeAutoOverlay />` as fallback. Both gated on `isGodModeReady && isEditMode`.

**Fix 2+4 — Schema Panel**: Full rewrite of `god-mode-schema-panel.tsx`. Dark purple theme `bg-purple-950/95`, w-64. Collapsible (side tab at `right-0 top-24` with "SCHEMA ◂"). Expanded panel at `right-4 top-16`. h-6 w-6 buttons with text symbols (−/+/↩). `effectivelyVisible` computed from `simulatedPermissions` (not `can()`).

**Fix 3 — SchemaGate**: Full rewrite of `schema-gate.tsx`. Always-visible border in edit mode with color coding: green=visible, orange=pending removal, blue=pending add, dashed rose=hidden. Content at opacity-40 when hidden (shows children dimmed). h-6 w-6 buttons with text symbols. `effectivelyVisible` from `simulatedPermissions`.

**Fix 5 — Page migrations**:
- `permission-schema.ts` — 5 new schemas: `/settings/members` (updated), `/settings/workspace`, `/settings/audit-logs`, `/settings/api-keys`, `/settings/access-control` → 14 pages total
- `workspace/page.tsx` — replaced 3 PermissionGates with SchemaGates + usePagePermissions
- `audit-logs/page.tsx` — added usePagePermissions (schema-only; page uses complex canViewAudit logic)
- `api-keys/page.tsx` — added usePagePermissions (all elements have permission: null)
- `access-control/page.tsx` — added usePagePermissions
- `settings-admin-views.tsx` (MembersView) — replaced PermissionGates with SchemaGates for invite_member and search_filters

**TypeScript**: `npx tsc --noEmit` — passed (0 errors)

**Build**: `npm run build` — running (background)

**Key decisions**:
1. Schema panel XOR auto-overlay — schema takes priority on pages with schema, auto-overlay fallback for unmapped pages (/flow, /docs etc.)
2. effectivelyVisible uses simulatedPermissions directly (reactive) — not can() which is a closure snapshot
3. Collapsible side tab preserves screen real estate during active testing
4. audit-logs + api-keys: usePagePermissions only (no SchemaGates) to avoid conflicting with page's own complex permission logic

---

## Task 17 — Fix Missing +/- Buttons on Roles Tab, Permissions Tab, Danger Zone [COMPLETED]

**Root cause**: BUG-047. All three elements had `permission: null` in `PAGE_SCHEMAS`. Panel guard `{el.permission && <button>}` → null → no button. SchemaGate `!element?.permission` guard → passthrough.

**Files changed**:
- `frontend/src/lib/permission-schema.ts` — `roles_tab: permission: null → "settings.role.view"`, `permissions_tab: null → "settings.permission.view"`, `danger_zone: null → "settings.organization.delete"` (type section→action)
- `frontend/src/app/settings/organizations/[id]/page.tsx` — wrapped `<SettingsDangerZone>` in `<SchemaGate elementKey="danger_zone">`

**TypeScript**: clean. **Build**: passed.

---

## Task 18 — Fix God Mode Exit Not Returning to Real Data [COMPLETED]

**Root cause**: BUG-048 (3-part). On `isGodModeReady → false`, real `contextQuery` data was stale; workspace store still held mock scope IDs (9001); god-mode tracker retained stale permission registrations.

**Fix 1 + 3** (`platformContext.tsx`):
- Added `const prevGodModeReady = useRef(false)` alongside `pathnameRef`
- Added `useEffect` on `[isGodModeReady, ...]`: on `true → false` transition, clears scope IDs (`setSelectedOrganization(null)`, `setSelectedWorkspace(null)`, `setSelectedProject(null)`), then calls `void contextQuery.refetch()`

**Fix 2** (`permission-simulator.ts`):
- Imported `useGodModeTracker` from `@/lib/god-mode-tracker`
- In `deactivateGodMode`: calls `useGodModeTracker.getState().clearAll()` before `set({ isDeactivating: true })`

**Files changed**:
- `frontend/src/context/platformContext.tsx` — 2 additions (ref + effect)
- `frontend/src/lib/permission-simulator.ts` — 2 additions (import + clearAll call)

**Build**: passed.
