# Docker Infrastructure

Current local development uses root `docker-compose.yml`.

This directory is reserved for future Docker standards, base images, compose overlays, and container build conventions.
